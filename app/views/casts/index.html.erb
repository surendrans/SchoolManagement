<% title "Casts" %>
<%= form_for @search, :as => :search, :html => {:id => "search-form", :method => :get}   do |f| %>
	<div id="div-search-form">
		<table>
			<tr><h3>Searching Criteria</h3></tr>
			<tr>
				<td></td>
				<td><%=f.submit, :name => nil%></td>
				<td><%= reset_form_values%></td>
			</tr>
		</table>
	</div>
	<div id='adv_trans'>
		<%= render 'adv_trans'%>
	</div>
<%end%>
